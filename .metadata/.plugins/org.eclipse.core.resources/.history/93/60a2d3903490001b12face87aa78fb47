package model.entities;

public class Duelist extends Agent {

	public Duelist() {
		super();
	}

	@Override
	public Double totalScore() {
		return (2 * entryFraggerScore + defenderScore + 1.5 * infoCatcherScore + infoDenierScore + 1.5 * mobilityScore + ultimateScore) / 8;
	}

}
